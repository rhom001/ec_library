#include <iostream>
#include <cstdlib> 
#include <unistd.h>
#include "Format.h"
using namespace std;

int main()
{
    /*
	string test;		//this portion is optional ... debug purpose  only
	bool b_exit = false;
	while(true)
	{
		ClearScreen();
		generic_guestmenu(b_exit);
		if(b_exit == true) break;		// exit program
		
		//this portion is optional ... debug purpose  only
		cout<<"Testing ... System Pause: Press any key and then enter"<<endl;
		cin >> test;	
	}
	ClearScreen();	//clean terminal screen
    */
	return 0;
}
